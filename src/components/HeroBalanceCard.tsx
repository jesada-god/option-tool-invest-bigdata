export function HeroBalanceCard({ label, value = '—', change, freshness = 'Data will connect in Phase 2' }: { label: string; value?: string; change?: string; freshness?: string }) { return <section className="card"><span className="muted">{label}</span><div className="hero-value">{value}</div>{change && <strong className={change.startsWith('-') ? 'danger' : 'positive'}>{change}</strong>}<p className="muted">{freshness}</p></section>; }
