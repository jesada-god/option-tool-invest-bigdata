import { ReactNode, useState } from 'react'; import { Sidebar } from '../components/Sidebar'; import { BottomNav } from '../components/BottomNav'; import { GlobalSearch } from '../components/GlobalSearch';
export function AppLayout({ children }: { children: ReactNode }) { const [open, setOpen] = useState(false); return <div className="app"><Sidebar openSearch={() => setOpen(true)} /><main className="main">{children}</main><BottomNav openSearch={() => setOpen(true)} />{open && <GlobalSearch close={() => setOpen(false)} />}</div>; }
